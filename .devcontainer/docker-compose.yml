name: gitblog-devcontainer
services:
    dev:
        build:
            context: ..
            dockerfile: .devcontainer/Dockerfile
        user: vscode
        volumes:
            - ../:/workspaces/dev
        command: ["tail", "-f", "/dev/null"]
